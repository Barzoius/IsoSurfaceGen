using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Vector3 vertexLerp(Vector3 p1, Vector3 p2, float v1, float v2, float isosurface)
//{
//    float t = (isosurface - v1) / (v2 - v1);
//    return p1 + t * (p2 - p1);
//}
